package mybookstore.model;

/**
 * Класс для перечисления статуса запроса на книгу.
 */
public enum RequestStatus {
    /**
     * статус запроса новый.
     */
    NEW,
    /**
     * статус запроса новый.
     */
    COMPLETED,
    /**
     * статус запроса новый.
     */
    CANCELLED;
}
